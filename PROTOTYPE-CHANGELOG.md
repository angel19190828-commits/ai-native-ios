# 原型变更记录

本文件记录 AI 原生 iOS 概念原型的主要修改。每条记录说明改动内容、改动原因，以及它对应的设计判断，方便后续制作 case study、演示文稿和面试讲述。

## Version 1 — 低保真交互验证

日期：2026-07-31  
原型：[prototype/wireframes.html](prototype/wireframes.html)

### 1. 从静态线框图改为可交互 iPhone 模拟

**改了什么**

- 建立 393 × 852 的 iPhone 设备框架。
- 将流程整理为 8 个可直接跳转的关键状态。
- 加入灵动岛、邮件、授权、后台规划、用户介入、计划确认、持续跟进和完成状态。

**为什么改**

静态页面无法验证 AI 何时出现、何时需要授权，以及后台任务如何向用户反馈进度。可交互版本可以直接检查这些行为是否连贯。

**设计判断**

这版主要验证系统行为和信息层级，不代表最终视觉。

### 2. 将灵动岛设为 AI 的轻量入口

**改了什么**

- 在邮件场景中加入 AI 脉冲指示。
- 灵动岛作为查看建议、后台进度和待处理事项的统一入口。
- 用户不需要离开当前 App 才能知道 AI 的状态。

**为什么改**

本项目不希望 AI 通过全屏界面打断用户。灵动岛可以让系统能力保持可见，同时把用户留在原本的任务环境中。

**设计判断**

AI 应该“在场但不占场”：普通状态保持轻量，只有关键决定才扩大界面。

### 3. 加入灵动岛展开 motion

**用户反馈**

灵动岛应该有展开的感觉，而不只是一个静态 AI 标记。

**改了什么**

- AI 脉冲出现后，灵动岛通过弹性动画展开。
- 增加展开、轻微回弹、悬停和按压反馈。
- 动画主要使用 `transform`、`opacity` 和宽度过渡。

**为什么改**

Motion 用来表达系统状态发生变化：AI 已从被动识别转为有一项建议等待用户查看。

### 4. 明确告诉用户 AI 已识别邮件

**用户反馈**

只显示 AI 标记时，不清楚系统是否正在识别邮件，也不知道下一步可以做什么。

**改了什么**

- 邮件出现约 0.7 秒后，灵动岛展开为：
  - 「发现一封面试邀请」
  - 「要规划吗？」
- 点击后显示「需要我帮你安排这场面试吗？」
- 主操作改为「查看规划建议」。
- 明确说明确认前不会执行任何操作。

**为什么改**

系统需要同时说明三件事：识别到了什么、能够提供什么帮助、是否已经开始执行。

**设计判断**

识别不等于执行。AI 可以主动提出建议，但开始规划仍由用户决定。

### 5. 将后台规划从独立页面改为浮窗

**用户反馈**

后台规划应该是浮窗，而不是进入一个新的完整任务页面。

**改了什么**

- 保留当前邮件作为背景。
- 用轻量浮窗显示当前阶段、整体进度和处理状态。
- 增加「收起浮窗」操作。
- 收起后任务继续运行，点击灵动岛可以重新打开浮窗。
- 普通阶段只显示当前重点，不再一次铺开五个完整步骤。

**为什么改**

如果后台规划占据整个屏幕，就与“不打断用户使用其他 App”的核心理念冲突。浮窗允许用户短暂查看过程，然后继续原来的操作。

**设计判断**

- 普通进度：灵动岛 + 可收起浮窗。
- 需要用户决定：升级为明显的操作面板。
- 已获得授权且不需要判断：继续后台执行。

### 6. 加入关键节点确认与后台阶段更新

**改了什么**

- 授权前显示邮件信息摘要、来源和所需权限。
- 后台流程拆分为理解邀请、检查日程、规划通勤、生成计划和等待确认。
- 日程冲突时暂停任务并要求用户处理。
- 计划确认页展示通勤计算、提醒和邮件草稿等拟执行操作。
- 完成后保留结果查看与撤销入口。

**为什么改**

用户授权 AI 工作，并不等于用户放弃控制。系统仍需持续说明进度，并在不可逆、敏感或存在冲突的节点请求确认。

## 后续记录格式

之后每次修改继续按以下格式追加：

### 改动标题

**日期**

YYYY-MM-DD

**用户反馈或触发原因**

说明为什么需要修改。

**改了什么**

- 具体界面、流程或文案变化。

**设计判断**

说明这项修改如何支持研究发现、六个系统行为维度或最终设计原则。

**待验证**

说明下一轮用户测试需要观察什么。

### 7. 让识别卡片直接下拉扩展为规划

**用户反馈**

「查看规划建议」应简化为「查看规划」；点击后不应切换到另一个底部面板，而应由当前卡片自动向下展开。

**改了什么**

- 主操作文案改为「查看规划」。
- 点击后，当前 AI 卡片通过下拉展开 motion 延伸为规划摘要。
- 规划摘要继续保留在邮件上方，展示识别信息、原文来源、将访问的系统能力和授权操作。
- 增加「返回」，允许用户收回展开状态。

**为什么改**

同一容器的连续扩展可以更清楚地表达：规划内容来自刚刚识别到的邮件，而不是跳转到一个无关的新页面。

**设计判断**

一次意图使用一个连续界面：先提出帮助，再展开细节，最后请求授权。

### 8. 分离授权前的规划范围与授权后的执行过程

**用户反馈**

Calendar、Maps 和 Reminders 是重要内容，需要明显呈现；同时需要判断它们应该出现在用户允许规划之前还是之后。

**改了什么**

- 授权前不再显示技术式 App 使用说明。
- 改为三个简短规划目标：检查日程冲突、计算出发时间、整理准备提醒。
- 保留一句边界说明：「现在只生成规划，执行任何更改前会再次确认。」
- 用户允许规划后，后台浮窗显示 Calendar → Maps → Reminders 横向任务轨道。
- 每张卡片分别显示等待、处理中或已完成状态，下一张卡片保持部分可见并支持横向滑动。

**为什么改**

授权前，用户需要理解允许 AI 做什么；授权后，用户才需要看到 AI 正在使用哪些系统能力以及当前进度。把两者分开可以减少说明文字，同时提升后台执行的透明度。

**设计判断**

结果范围先于技术实现；系统执行细节在授权后按阶段渐进披露。

### 9. 为 AI 建议卡补充进场 motion

**用户反馈**

灵动岛展开后出现的 AI 建议卡是瞬间显示，与其他状态的 motion 不一致。

**改了什么**

- 建议卡从灵动岛下方淡入、下移并轻微回弹。
- 卡片由收拢状态展开，强化它与灵动岛的来源关系。
- 标题、说明和操作按钮依次出现，帮助用户按顺序阅读。

**为什么改**

进场动画不仅用于装饰，也用于解释建议卡来自灵动岛，并引导用户从识别结果进入规划操作。

**设计判断**

普通建议采用轻量弹出；规划摘要向下扩展；必须处理的冲突才升级为强提醒面板。

### 10. 移除后台阶段的手动推进

**用户反馈**

用户授权规划后，不需要反复点击「推进阶段」；AI 应自动完成普通规划步骤。

**改了什么**

- 移除后台浮窗中的「推进阶段」按钮。
- AI 自动依次处理 Calendar、Maps 和 Reminders。
- 浮窗显示「AI 将自动继续」，用户仍可收起并继续使用当前 App。
- 发现日程冲突时自动暂停，等待用户决定。
- 最终执行系统更改前仍保留用户确认。

**为什么改**

授权的意义是允许 AI 代替用户完成中间整理工作。如果每一步仍要求点击，AI 只是在展示流程，而没有真正减少操作负担。

**设计判断**

自动推进普通、可恢复的步骤；仅在冲突、敏感操作或正式写入前请求用户确认。

### 11. 统一冲突提醒并定义无回应策略

**用户反馈**

日程冲突面板应与前面的黑色 AI 卡片保持一致；冲突发生时灵动岛需要明确提醒，同时需要定义用户长时间没有回应时系统怎么处理。

**改了什么**

- 日程冲突改为从顶部出现的黑色 AI 卡片。
- 灵动岛显示「日程有冲突」。
- 冲突卡片说明 AI 已暂停后续规划，不会自动替用户选择。
- 演示中用户 5 秒未回应，卡片自动收起，灵动岛保留「日程有冲突 · 待处理」。
- 点击灵动岛可以重新打开冲突卡片。

**为什么改**

冲突属于必须由用户判断的关键节点，但强制让面板长期占据屏幕同样会打断用户。将任务暂停并收起到灵动岛，可以同时保留控制权与任务状态。

**设计判断**

无回应不等于同意。系统保存状态、停止后续写入并稍后提醒一次；它不会自动移动、删除或保留冲突事件。

### 12. 统一最终计划与后台步骤的布局

**用户反馈**

最终计划不应突然切换为底部 sheet，而应沿用前面步骤的卡片布局。

**改了什么**

- 最终计划改为顶部黑色 AI 卡片，并使用相同的下拉展开 motion。
- Calendar、Maps、Reminders 继续以横向任务轨道呈现，全部标记为已完成。
- 通勤计算保留在同一卡片中，突出建议出发时间。
- 「确认并执行」仍是正式写入系统前的关键确认。

**为什么改**

统一容器能让用户清楚理解最终计划是同一个后台任务的结果，而不是进入了新的功能或页面。

**设计判断**

同一任务沿用同一界面结构，只通过状态、内容和操作级别表达阶段变化。

### 13. 新增冲突处理状态与具体方案

**用户反馈**

「调整计划」不能直接跳过冲突进入最终计划，需要先让用户明确选择如何解决冲突。

**改了什么**

- 「调整计划」改为「查看其他方案」。
- 新增独立的「冲突处理」状态。
- 提供三个具体选择：联系 Alex 改期、保留面试并处理课程、稍后决定。
- 选择联系 Alex 后，AI 展开一封改期草稿，但不会自动发送；Maps 与 Reminders 继续暂停，直到新时间确定。
- 选择保留面试后，AI 才继续完成 Maps 与 Reminders。
- 选择稍后决定时，任务保持暂停并收起到灵动岛。

**为什么改**

模糊的「调整计划」没有说明用户将改变什么，也错误地允许流程跳过未解决的日程冲突。具体方案能够展示选择后果，并保持信息依赖关系正确。

**设计判断**

AI 可以准备选项和草稿，但不能在缺少新时间或用户选择时继续依赖这些信息的后续规划。

### 14. 持续跟进浮窗自动收起

**用户反馈**

Calendar、Maps 和 Reminders 全部完成后，持续跟进浮窗不应继续占据当前 App。

**改了什么**

- 持续跟进浮窗显示完成状态约 2.8 秒后自动收起。
- 收起后灵动岛继续显示「持续跟进 · 3/3」。
- 点击灵动岛可以重新打开浮窗；重新打开后仍会自动收起。
- 浮窗收起前显示「即将自动收起」。

**为什么改**

完成状态只需要短暂确认，长期状态由灵动岛承载，避免后台任务持续遮挡用户正在使用的 App。

**设计判断**

短期反馈使用浮窗，长期状态留在灵动岛；用户可以按需重新展开。

### 15. 浮窗通过反向 motion 回收到灵动岛

**用户反馈**

浮窗自动收起时不应直接消失，而应通过 motion 表达它回到了灵动岛。

**改了什么**

- 浮窗收起时向上移动、缩小并逐渐淡出。
- 裁切范围同步收拢，使视觉终点指向灵动岛。
- 自动收起和手动点击「收起浮窗」共用相同动画。
- 动画完成后才移除浮窗内容，灵动岛继续显示后台状态。

**为什么改**

反向 motion 能说明任务没有结束，只是从浮窗状态转为由灵动岛持续承载。

**设计判断**

展开表示需要查看，回收表示转入后台；motion 用来解释界面状态之间的连续关系。

### 16. 简化授权前的规划标题

- 将「AI 将为你准备」改为「为你准备」。
- 减少重复强调 AI，让注意力集中在用户将获得的三个规划结果上。

### 17. Case study 同步四位用户测试结果

- 研究方法由 2/3 场初步结果更新为 4/4 场探索性分析。
- 新增 User 3 与 User 4 的行为路径、证据和报告链接。
- 跨案例结论增加手动复核的认知价值、地图来源、提醒冗余与来源完整性。
- 六维度强度和机会评分按四场证据重新计算。
- 核心设计问题由“后台自动组织”修正为“减少机械搬运，同时保留有意义的复核与判断”。
- 风险说明改为：现有流程已测试，原型理解与信任仍待下一轮验证。

### 18. 新增 Approval-first AI 独立原型

- 新建 Approval-first 原型文件，不覆盖后台自动版本。
- 移除所有阶段自动推进、冲突超时和持续跟进自动收起。
- Calendar、Maps、Reminders 每一步保持停留并等待明确批准。
- 调整冲突解决后的阶段映射，避免跳过 Maps 或 Reminders。
- 保留后台自动版作为 Delegated AI 对照版本。
- Case study 低保真章节改为两个版本的比较，并增加独立入口。

### 19. Approval-first 授权浮窗统一为黑色 AI 界面

- 将逐步授权阶段的浮窗改为与邮件建议、摘要和冲突处理一致的黑色 AI 界面。
- 删除重复的「AI 请求下一步授权」标签；具体阶段标题与「允许检查日程／允许规划路线／允许设置提醒」按钮已经充分表达授权状态。
- 保留「等待批准」、完成进度和系统名称，让用户仍能快速判断当前停在哪里、接下来会授权什么。

### 20. 新增面试当天与 Maps 导航状态

- 从 Approval-first 流程中移除 Reminders；规划范围收敛为 Calendar 与 Maps。
- 新增「面试当天」状态，展示用户打开手机后看到的时间、Calendar 事件与基于实时交通生成的出发建议。
- 新增「开始导航」交互，进入 Maps 导航画面并由灵动岛持续显示预计到达时间。
- Calendar 负责事件与时间基准，Maps 负责动态出发与导航，AI 负责连接和呈现状态。
### 21. Approval-first 通勤决策与分层提醒流程

- 修改文件：`prototype/wireframes-approval.html`。
- 将路线直接设计为决策界面：用户先确认出发点，再在步行、自行车、公交、开车之间切换；小地图、路线、通勤时长、预计到达和计划出发时间同步更新。
- 将出发结果统一表述为「计划 9:35 出发」，并在次级区域展示邮件提前到达要求、Maps 通勤时长与可修改的 AI 缓冲建议。
- 通勤计划确认后才进入提醒设置；先询问准备时间，再分别生成 Clock 起床、Reminders 准备事项、时效出发通知和交通方式相关的动态通知。
- 公交、开车、步行和骑行分别使用到站/延误、拥堵/停车、天气/步行和天气/骑行提醒逻辑。
- 灵动岛在通勤确认后仅显示「通勤计划已完成 · 计划 9:35 出发」；关键页面不自动跳转，保留逐状态审查。
- 增加直接从状态导航进入后期页面时的审查默认值，避免未定义内容；正常流程仍要求用户明确确认出发点与准备时长。
- 验证结果：脚本语法通过，16 个交互目标均有处理路径，12 个审查状态和 12 组路线时间计算通过。
### 22. 通勤规划按 Persona 简化信息层级

- 修改文件：`prototype/wireframes-approval.html`。
- 将通勤规划首屏收敛为出发点、紧凑交通方式、小地图、计划出发时间、预计到达和确认操作。
- 删除首屏重复解释，将完整计算依据默认折叠到「为什么是 9:35？」入口中。
- 展开后仍可核对邮件提前到达要求、Maps 通勤时长与可修改的 AI 缓冲建议，兼顾低认知负担和可解释性。
- 提醒内容继续保持在确认通勤后的下一状态，没有混入路线决策界面。
- 验证结果：脚本语法、折叠/展开状态、缓冲调整入口和提醒内容隔离均通过。
### 23. 通勤规划改为三步 Chatbox 流程

- 修改文件：`prototype/wireframes-approval.html`。
- 将单个高密度通勤面板拆为三个停留状态：确认出发点 → 选择交通方式 → 查看通勤结果。
- 用户选择出发点后切换到下一题；交通方式按钮只显示图标，AI 推荐通过说明文字和公交图标上的蓝点表达。
- 最终结果首层只显示计划出发时间、通勤时长与预计到达时间；点击结果 banner 才展开完整计算依据。
- 路线默认收起为 Map placeholder，用户点击「路线预览」后才展开小地图。
- 保留返回修改出发点、修改交通方式、调整缓冲和确认通勤计划的路径。
- 验证结果：脚本语法、三步切换、纯图标模式、结果折叠和地图折叠逻辑通过。
### 24. P0 信任、控制与任务状态修正

- 修改文件：`prototype/wireframes-approval.html`。
- 在规划授权后新增只读预检，先检查邮件是否自带 Add to Calendar、Calendar 是否已有重复事件，以及是否存在日程冲突，避免重复创建。
- 将「地点与要求」改为「要求处理状态」，逐项显示时间、地点、提前到达和作品集的来源、处理去向及遗漏状态。
- 主流程收敛为两次主要确认：开始规划与最终执行；普通只读检查不再逐步索要权限，只在冲突等异常处打断。
- 提醒设置改为最小推荐组合：Reminders 管准备事项、时效通知管出发、交通通知只在变化时出现；Calendar 不重复提醒，Clock 起床闹钟默认关闭并允许按需添加。
- 在执行结果中新增 Calendar 稳定重新入口「面试事件 → 查看面试计划」，不再只依赖灵动岛。
- 将完成语义统一为「系统设置已完成」，并分开展示已设置、等待中和用户仍需完成的事项。
- 验证结果：脚本语法、P0 状态、主流程顺序、条件闹钟、稳定重新入口和完成语义均通过；旧 Calendar 单步授权状态及「面试准备已完成」已清除。
### 25. 删除重复预检屏并合并防重复结果

- 修改文件：`prototype/wireframes-approval.html`。
- 删除独立的「重复与冲突预检」状态，避免与后续「冲突处理」重复展示同一冲突。
- 保留防重复检查逻辑，并在「要求处理状态」顶部用一行显示「已检查：没有重复事件」。
- 用户点击「开始规划」后直接进入要求处理状态；真正的日程冲突只在下一状态中集中处理。
- 状态导航由 12 项收敛为 11 项。
- 验证结果：内联 JavaScript 语法、状态移除、按钮目标、无重复事件提示和冲突处理保留均通过。
### 26. 进一步精简要求处理状态

- 修改文件：`prototype/wireframes-approval.html`。
- 删除「已检查：没有重复事件」可视提示；防重复保留为后台规划逻辑，不占据用户界面。
- 删除「4/4 项均有处理方式 / 没有发现未处理要求」总结卡。
- 灵动岛改为「邮件信息已整理 · 等待规划」，避免系统式审计文案。
- 主按钮由「继续处理日程冲突」改为「开始规划」；冲突在下一状态中再单独呈现。
- 同步清理已删除组件的无用 CSS。
- 验证结果：JavaScript 语法、旧组件移除和新按钮目标通过。
### 27. 收敛重复信息、动作与主要状态

- 修改文件：`prototype/wireframes-approval.html`。
- 主要状态由 11 个减少为 9 个；「其他方案」保留为冲突处理的条件展开内容，不再占据主导航。
- 「开始规划」只保留在规划授权；邮件核对屏按钮改为「继续」，并删除提前出现的 Calendar/Maps 处理去向。
- 邮件核对屏只显示日期、地点、提前到达和作品集四项事实。
- 提醒设置按钮由「确认提醒设置」改为「继续」；交通变化与“该出发了”合并为同一条动态出发通知。
- 合并「执行结果」与「系统设置完成」，在同一屏展示已设置、等待当天更新和用户待完成，并保留 Calendar 重新入口。
- 主要确认收敛为一次「开始规划」和一次「确认并执行」。
- 验证结果：JavaScript 语法、9 个主要状态、单次规划授权、单次执行确认和合并完成状态通过。
### 28. 邮件提取信息支持修改

- 修改文件：`prototype/wireframes-approval.html`。
- 在「核对邮件信息」右上角新增轻量「修改」入口，默认状态仍保持简洁列表。
- 进入编辑状态后可修改日期与时间、地点、提前到达要求和准备要求，并支持保存或取消。
- 保存后回到核对状态，并提示后续规划将使用更新后的信息。
- 修改提前到达分钟数后，计划出发时间会随之重新计算。
- 对输入内容进行 HTML 转义，避免编辑值破坏界面结构。
- 验证结果：JavaScript 语法、四个编辑字段、保存/取消处理及出发时间重算逻辑通过。
### 29. 完成状态统一为黑色展开卡片

- 修改文件：`prototype/wireframes-approval.html`。
- 删除「系统设置完成」的浅色全屏呈现，底层恢复保留 Mail。
- 将已设置、等待当天更新、用户待完成、Calendar 重新入口和操作按钮统一放入黑色展开卡片。
- 继续保留「系统设置完成不等于面试准备完成」的状态区分。
- 验证结果：JavaScript 语法、Mail 底层、黑色 overlay、重新入口与操作按钮通过。

## 30. 邮件信息修改贯穿后续流程（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 「核对邮件信息」保存后会清除依赖旧信息的准备偏好，并从更新后的事实重新规划。
- 日期与时间：重新判断课程冲突，更新冲突卡、出发时间、最终计划、当天状态与导航预计到达。
- 地点：同步到通勤摘要、Maps 执行内容、最终计划、当天事件与导航目的地。
- 提前到达要求：用于重新计算计划出发时间，并在计算依据中显示更新后的分钟数。
- 准备要求：同步到提醒、最终计划、完成状态与面试当天。
- 验证：内联 JavaScript 语法通过；关键状态不再依赖硬编码的 10:30 出发计算。

## 31. 完成状态信息减量（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 将「系统设置已完成」改为更具体的「面试计划已准备好」。
- 删除重复的「已设置 / 等待当天更新 / 你还需要完成」三层状态和 Calendar 重复入口。
- 仅保留 Calendar 时间、计划出发时间和一条用户待办。
- 主要操作改为「返回邮件」，次要操作保留「修改计划」。
- 灵动岛同步显示「面试计划已设置 · 计划出发时间」。

## 32. 增加无冲突日程路径（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 第 04 状态由固定的「冲突处理」改为中性的「日程检查」。
- Calendar 检查有时间重叠时，才进入原有冲突处理与其他方案。
- 没有时间重叠时，显示「这个时间没有冲突」，并允许直接继续规划通勤。
- 用户仍可返回修改邮件信息，避免无冲突结果成为单向路径。

## 33. 持续任务、执行恢复与导航闭环（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 「稍后决定」进入可恢复的暂停任务；邮件内卡片与灵动岛都可重新进入冲突处理。
- 「保存改期草稿」进入「等待 Alex 回复」持续状态，可查看草稿或继续按当前时间规划。
- 最终确认后新增执行中状态，逐项显示 Calendar、Maps 与 Reminders 写入结果。
- 新增部分失败状态：保留成功项、仅重试失败项，或撤销已经完成的操作。
- 新增撤销确认与撤销完成状态，明确原始邮件不会被删除。
- 新增执行后的独立「任务详情」，替代重新进入最终确认页；支持查看已创建内容、修改与撤销。
- 路线详情增加分段行程；通知规则采用按需展开，说明触发阈值、交通异常和勿扰模式。
- 导航增加「结束导航」以及到达状态，停止实时追踪后可返回任务详情。
- 左侧状态导航新增执行中、任务详情、部分失败与导航结束，便于逐屏审查。
- 验证：全部内联 JavaScript 语法通过，暂停、等待、重试、撤销和导航结束入口均存在。

## 34. AI 推荐的偏好设定（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 第 06 步由「提醒偏好」改为「偏好设定」，删除先询问准备时长的固定流程。
- AI 根据用户常用 Calendar、Reminders、公共交通，以及很少使用 Siri/Shortcuts 的行为给出初始建议。
- 默认开启 Reminders 准备事项和动态出发提醒；Calendar 重复提醒与 Clock 闹钟默认关闭。
- 四项偏好均可单独切换，AI 只推荐、不替用户永久决定。
- 第 05 步明确交通方式推荐来自常用方式与当前路线。
- 偏好选择同步进入最终确认、执行中和执行后的任务详情。

## 35. 渐进式自主主流程（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 原型由「逐步授权、逐屏停留」改为「AI 默认推进，只在关键节点询问」。
- 规划授权后新增「AI 综合规划」：一次连接邮件、Calendar、Maps 与使用偏好。
- 邮件核对、通勤选择和偏好设定不再是主流程必经状态，改为综合建议与最终计划中的按需修改入口。
- 有日程冲突时进入异常处理；无冲突时直接进入最终计划。
- 用户确认保留面试后直接生成最终计划，不再重新经历通勤与偏好问答。
- 最终确认改为唯一一次执行确认；发送、删除和后续修改仍单独询问。
- 主状态导航收敛为：邮件识别 → 规划授权 → AI 综合规划 → 异常处理 → 最终确认 → 执行中 → 任务详情 / 恢复状态 → 面试当天 → 导航结束。
- 旧的详细核对、通勤和偏好页面仍保留，作为 refine 工具。
- 验证：内联 JavaScript 语法通过；主导航不再包含三个逐项询问状态。

## 36. 灵动岛循序规划与独立偏好设置（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`、`prototype/intelligence-settings.html`
- 主流程改为灵动岛循序更新：提取邮件 → 检查 Calendar → 规划通勤 → 设置提醒。
- 后台处理时不展开完整结果；只有日程冲突等需要用户决定的状态才弹出黑色决策卡。
- 冲突处理后卡片收起，AI 根据常用出发点与公共交通偏好继续规划。
- 通勤与提醒中间结果不再逐屏展示，统一进入最终计划。
- 主状态导航精简为 8 个阶段，详细修改页面不再占用主流程。
- 执行中移除「模拟失败」与「全部成功」按钮，改为灵动岛自动显示写入进度并进入已设置状态。
- 已设置页面不再返回邮件，主要动作改为进入面试当天。
- 新增 Apple Intelligence 设置页：常用交通方式、常用出发点、Calendar、Reminders、动态出发提醒与可选 Clock 闹钟。
- 验证：两个页面的内联 JavaScript 语法通过；主导航、顺序状态、设置页链接与自动执行路径存在。

## 37. 旧版摘要卡片采用新版布局（2026-07-31）

- 修改文件：`prototype/wireframes.html`
- 保留旧版完整流程，仅更新「摘要与授权」卡片。
- 标题改为「请确认这些信息」，合并日期与时间，集中显示地点、提前到达和准备要求。
- 删除同屏的日程 / 路线 / 提醒三张预告卡，减少授权前的信息密度。
- 主操作改为「信息正确，开始规划」，并明确此时只生成规划，执行前仍会确认。
- 验证：内联 JavaScript 语法通过，旧摘要文案和 scope-strip 已移除。


## 38. 灵动岛阶段状态与动效落地（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 灵动岛按状态平滑改变宽度，但保持高度、位置与 scale 稳定，避免整颗岛反复放大缩小。
- 阶段文字使用横向位移、淡入与轻微模糊消散动效。
- 处理中使用白色呼吸圆点；完成阶段短暂显示绿色勾号；需要决定时持续显示珊瑚色圆点。
- 四阶段按 1/4–4/4 在最右侧显示：提取邮件、检查 Calendar、规划通勤、设置提醒。
- 只有需要用户决定的冲突状态才展开黑色决策卡。
- 修改前备份：`prototype/wireframes-approval.pre-island-motion.bak.html`。


## 39. 最终计划确认卡重设计（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 参考用户提供的最终确认设计，重构最终计划卡片的信息层级。
- 将“计划几点出发”设为唯一主视觉，交通方式、时长和预计到达作为次级信息。
- 执行内容压缩为日历、准备提醒、交通变化通知三条，删除重复地址、状态标签和权限说明。
- 底部只保留“确认并执行”和“调整”；调整进入综合计划修改，而非仅修改提醒。
- 修改前备份：`prototype/wireframes-approval.pre-final-plan-redesign.bak.html`。


## 40. 最终计划与已设置状态分离（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 保留最终计划卡片的现有视觉结构，明确标记为“尚未执行”。
- 三项内容改为未来动作：创建日程、设置作品集提醒、开启交通变化通知。
- 绿色完成勾改为蓝色纳入计划标记，避免与执行成功混淆。
- 用户确认后卡片收起，执行过程仅通过灵动岛依次显示三项写入进度。
- 每项执行完成短暂显示绿色勾号，全部完成后才进入“已设置”结果页。
- 修改前备份：`prototype/wireframes-approval.pre-final-plan-flow.bak.html`。

- 最终计划标题移除星光装饰，仅保留状态文字。


## 41. AI 推测式通勤确认与已设置页面收敛（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 通勤设置由连续表单改为 AI 先推测“出发地点 + 交通方式”的轻量建议卡。
- 默认显示从家乘公交的建议、计划出发时间和预计到达时间；用户可直接继续。
- 只有点击“调整”后才同时展开常用地点与交通方式，选择后即时重算。
- 已设置页面改用普通用户语言“面试已安排”，删除 Calendar 以下的通勤、准备事项和动态提醒列表。
- 已设置页仅保留日历结果、查看当天安排、调整与撤销入口。
- 修改前备份：`prototype/wireframes-approval.pre-commute-task-simplify.bak.html`。

- “撤销安排”移至已设置卡片底部，以分隔线与主操作区隔开并居中显示。


## 42. 通勤建议接入后台主流程（2026-07-31）

- 修改文件：`prototype/wireframes-approval.html`
- 修复此前只改通勤编辑页、后台主流程仍自动跳过确认的问题。
- 主路径现在为：日程检查 → 生成通勤建议 → 轻量通勤确认 → 设置提醒 → 最终计划。
- AI 先推测“从家乘公交”，用户可直接继续或展开调整地点与交通方式。
- 用户确认通勤后卡片收起，提醒设置仅在灵动岛完成，再进入最终计划。
- 修改前备份：`prototype/wireframes-approval.pre-main-commute-confirm.bak.html`。


## 43. 六阶段导航、Home Screen 与来源权限（2026-07-31）

- 主状态导航收敛为：发现邀请、AI 规划中、需要决定、最终计划、面试已安排、面试当天。
- 增加独立且不编号的 Home Screen 系统呈现入口，以及面试准备 Widget。
- Widget 作为灵动岛消失后的稳定重新进入路径，点击进入面试已安排页面。
- AI 规划中点击灵动岛可查看当前阶段与已完成阶段，不中断后台计时。
- 面试已安排页面增加溢出菜单：调整、来源与权限、管理通知、撤销。
- 最终计划也提供“来源与权限”二级入口。
- 来源 Sheet 显示本次 Mail、Calendar、Maps 的读取/写入范围，并链接长期权限设置。
- 第三方能力示例区分完整支持、有限支持和无法执行，并标记不属于本次任务。
- 修改前备份：`prototype/wireframes-approval.pre-home-privacy.bak.html`。


## 44. 最终计划项目可交互（2026-07-31）

- 最终计划的日历、作品集提醒和交通通知三行改为完整可点击区域。
- 增加 hover、按压、键盘焦点和右侧箭头反馈；蓝色标记继续表示已纳入计划。
- 日历项目可修改日期、时间和地点。
- 作品集提醒可选择提前 15、30 或 60 分钟，也可关闭。
- 交通通知进入 AI 通勤建议，可调整出发地和交通方式。
- 修改前备份：`prototype/wireframes-approval.pre-plan-item-interactions.bak.html`。

- 使用通用“准备事项提醒”替代硬编码的“作品集提醒”；具体提醒内容由邮件提取并支持用户修改。


## 45. 最终计划改为勾选控件（2026-07-31）

- 最终计划三行恢复为静态摘要，不再让整张 banner 可点击。
- 仅右侧蓝色圆形勾号可点击，用于加入或移出本次执行。
- 删除行末箭头；详细内容继续通过底部“调整”修改。
- 未勾选项目保留在列表中但降低透明度，用户可以重新开启。
- 执行阶段根据实际勾选项目动态生成任务，不再固定执行三项。
- 修改前备份：`prototype/wireframes-approval.pre-plan-selection-controls.bak.html`。

- 最终计划中的提醒与交通通知改为助手对用户的自然承诺语气，并根据交通方式动态解释提醒原因。


## 46. 调整入口移除旧综合分析页（2026-07-31）

- 最终计划、面试已安排和任务菜单中的“调整”不再进入旧综合分析页。
- 新增简洁调整菜单：日历、通勤、提醒。
- 三项分别进入对应的修改界面，取消返回最终计划。
- 旧分析状态保留在代码备份中，但用户主流程已不可达。
- 修改前备份：`prototype/wireframes-approval.pre-adjust-menu.bak.html`。

- 最终计划支持点击整行修改内容，同时保留右侧蓝色勾号为独立执行开关；勾号点击不会触发行编辑。

## 47. 后台规划节奏与犹豫恢复（2026-08-01）

- 将 AI 规划由约 2–3 秒的快速演示放慢为约 8–12 秒的分阶段过程：读懂邀请、检查日程、规划通勤、整理安排。
- 灵动岛在处理中显示当前阶段与 `1/4–4/4`；点击后仅展开已完成阶段、当前阶段及简短可验证说明，不展示内部推理。
- 正常规划不主动弹出进度卡；只有检测到日程冲突时进入「需要决定」。
- 在第一层冲突卡直接增加「稍后决定」，避免犹豫中的用户被迫进入更多方案或立即选择。
- 选择「稍后决定」后，决策卡收起，灵动岛持续显示「日程冲突待处理」；点击灵动岛可恢复同一处理上下文。
- 暂停期间不继续规划、不创建日历或提醒；恢复后从冲突后的通勤阶段继续，而不是重新读取邮件。
## 48. 草稿保存反馈（2026-08-01）

- 联系 Alex 的改期草稿保存后，不再直接跳入完整等待状态。
- 新增轻量通知「草稿保存成功」，并明确标注「尚未发送给 Alex」。
- 通知提供「查看」操作，可重新打开已保存草稿。
- 草稿页的文字「返回」按钮改为圆形返回图标，并补充无障碍标签、键盘焦点和按压反馈。
## 49. 移除重复的等待回复卡（2026-08-01）

- 保存改期草稿后回到邮件，不再自动展示大型「持续任务 · 等待回复」卡。
- 页面只保留轻量保存成功通知与灵动岛「等待 Alex 回复 · 草稿已保存」。
- 后续点击灵动岛重新显示同一轻量通知，可直接查看草稿，不再重复解释等待状态或要求用户再次选择。
## 50. 同步 Screen Review 网格（2026-08-01）

- 页面标题更新为「AI 轻量交互原型」。
- 首次规划说明中的地点、Calendar 冲突和通勤时间改为重点加粗。
- 暂时移除侧栏 Home Screen / Widget 入口。
- 将旧版 V1 验证重点更新为当前验证重点：轻量进度、减少打扰、异常中断、最终执行确认。
- 页面下方新增六个关键 screens 的审查网格：发现邀请、AI 规划中、需要决定、最终计划、面试已安排、面试当天。
- 网格通过同一 HTML 的嵌入状态实时渲染，不复制界面代码；原型修改会自动同步到网格。
- AI 规划 screen 在网格中冻结为可批注状态，避免审查时自动跳转。
## 51. 最终计划对齐与已安排状态说明（2026-08-01）

- 依据参考截图重新校准最终计划的三列布局：左侧图标、中间主副文案、右侧勾选控制统一垂直居中。
- 缩小右侧控制区宽度，统一图标尺寸、行高、行间分隔与列表内边距，减少视觉漂移。
- 「面试已安排」不再只显示单一日历结果；改为区分「面试已加入日历」「当天更新出发时间」「你还需要准备」三类信息。
- Screen Review 继续通过独立 query state 渲染 04 最终计划、05 面试已安排、06 面试当天，不复制静态界面。
## 52. 独立高保真原型与 Figma iPhone Assets（2026-08-01）

- 新建 `prototype/wireframes-high-fidelity.html`，不覆盖现有低保真 `wireframes-approval.html`。
- 保留原有六个状态导航与灵动岛状态逻辑；仅重建 iPhone 设备框架、系统材质、卡片、按钮、列表与 App icon 视觉。
- 视觉基于用户提供的 Figma `Iphone Assets` 文件：Home Screen、Notifications 与 System List。
- Figma 导出与原始图标保存到 `prototype/assets/figma-iphone/`，高保真文件使用本地资源，避免临时链接失效。
- 首次规划利益点改为「看清面试时间和地点」「提前发现日程冲突」「算好几点出门」，不再描述 AI 的内部工作步骤。
## 53. 高保真灵动岛展开与图标校正（2026-08-01）

- 高保真灵动岛在 AI 规划中可点击向下展开进度面板，再次点击或点击收起即可回到轻量状态。
- 展开面板与岛体视觉连接，并加入从灵动岛向下展开的缩放、位移和模糊过渡。
- 通过 Figma 导出图像像素匹配校正 App icon：Calendar 使用 `raw-17.png`、Reminders 使用 `raw-09.png`、Maps 使用 `raw-18.png`。
- Figma 资源库没有独立 Bus/Transit 组件；最终计划的公交方式改用 iOS/SF Symbols 风格的本地图形，不再使用 emoji。
## 54. 高保真灵动岛宽度与文字动效修复（2026-08-01）

- 修复高保真视觉层将灵动岛宽度固定为 `132px`、覆盖原型状态宽度，导致“调整计划 / 选择要修改的内容”等文案被截断的问题。
- 恢复原型的状态宽度规则：普通、提示、规划进度、阶段完成、需要决定分别使用对应宽度，不再把所有状态塞进同一窄岛。
- 保留原型的 motion 顺序：岛体先平滑扩宽，状态文字再淡入；切换为短状态时再平滑收窄。
- 仅保留单行省略作为极端长文案的安全兜底，正常状态应完整显示核心状态文字与右侧计数。

## 55. 高保真交通与地点图标及审查网格修复（2026-08-01）

- 从 Figma `Iphone Assets / icons` 页面导出并使用 `directions_walk`、`directions_bike`、`directions_bus`、`directions_car`，替换 emoji 与本地手绘交通图形。
- 家、学校、当天位置分别使用同一 Figma 页面中的 `Navigation / House_01`、`fi-rr-school`、`Navigation / Map_Pin` 图标。
- 最终计划主结果右侧图标会随当前交通方式同步变化；通勤调整选项和相关详情复用同一套 Figma 图标。
- 修复 Screen Review 中「AI 规划中」展开内容被嵌入容器裁切：缩小嵌入设备比例并增加可视高度，不改变主原型状态导航。


## 56. 状态 05 安全区域与 Figma 状态图标

- 高保真「面试已安排」卡片移除意外的横向滚动，并限制内容在 iPhone 安全区域内。
- 右上菜单按钮回收到卡片网格，不再超出设备框架。
- 日历、当天更新与准备事项分别改用 Figma icons 页的 Calendar、Bell、Noted 图标。
- 灵动岛改为左侧固定「呼吸圆点 + AI」，状态消息与计数在右侧展开，保留原有宽度动画逻辑。
- 修改文件：`prototype/wireframes-high-fidelity.html`；新增资源：`status-calendar.svg`、`status-bell.svg`、`status-note.svg`。


## 57. 暂停卡层级与 Material Reference 统一

- 「日程冲突还没有处理」卡片上移到灵动岛下方，减少无意义间距。
- 主操作「继续处理」居中保留实体按钮；次操作改为卡片下方的无容器「忽略」。
- 取消、调整、忽略等低优先级操作统一为文字操作，不再使用填充按钮容器。
- 高保真浮层和灵动岛采用 Figma Material Reference 的半透明黑色、背景模糊、23px 圆角与细描边语言。
- 修改文件：`prototype/wireframes-high-fidelity.html`。

## 58. 后台规划持续状态与安全区域修复（2026-08-01）

- 修复 `wireframes-high-fidelity.html?screen=processing` 进入后人为清空计时器、导致规划永久停住的问题。
- 展开进度浮窗时，阶段完成、打勾和下一阶段会实时刷新；收起仅隐藏浮窗，不暂停后台规划。
- 规划完成后继续进入现有异常判断或下一步，不要求用户保持浮窗打开。
- 将规划浮窗约束在 iPhone 安全区域内，并限制最大高度，避免导航内容横向飞出或被设备框裁切。

## 59. 修复规划卡死、面板闪烁、灵动岛阴影与手机外框比例（2026-08-01）

- 修改文件：`prototype/wireframes-high-fidelity.html`。
- **规划卡在 2/4 不动**：`hasConflict()` 及其依赖的 `eventTime`/`eventStart`/`eventEnd`/`timeToMinutes`/`fmt`/`arrivalMinutes`/`depart`/`arrive`/`eventDateLabel`/`liveArrival`/`esc`/`update` 等函数在从 `wireframes-approval.html` fork 出高保真原型时被遗漏，导致 `hasConflict()` 抛出 `ReferenceError`，后续阶段的定时器永远不会被注册。已从姊妹文件对齐补回全部缺失函数（依赖的 `facts`/`modes`/`origin`/`buffer`/`S`/`info` 等数据结构在高保真文件中本就存在，未改动）。修复后，无冲突路径能自动走完 1/4→4/4，冲突路径能在 5 秒左右正确弹出「需要决定」。
- **面板闪烁**：`showProgress()` 原本在每次阶段更新时都整体重建 `ov.innerHTML`，重新触发进场动画。现在拆分为「首次挂载」（`showProgress`，写入 `data-step` 标记）与「原地同步」（新增 `updateProgressPanel`，只更新对应 `.progress-row` 的 class、勾选符号与说明文字，以及顶部 `x/4` 计数），面板 DOM 节点在整个规划过程中保持同一个，不再整体消失重出。
- **灵动岛下多余阴影**：`.high-fidelity .island-sheet:before` 桥接矩形与实际生效的 `.screen .progress-pop.island-sheet{top:58px}` 位置对不上，产生一条错位色块。删除该 `:before`，改为让面板顶部直接以 `top:54px`（= 灵动岛展开态 `top:11px + height:43px`）与灵动岛贴合，面板顶部圆角改为 `11px 11px 28px 28px`，与灵动岛展开态底部圆角一致，视觉无缝衔接。
- **手机外框比例**：改用苹果公开的 iPhone 15/16 Pro 逻辑分辨率精确重算，不依赖任何 Figma 图片素材。`.screen` 靠 `.phone` 内容区自动算出 393×852pt（原来 `.phone` 外框 402px 宽与 874px 高的比例不匹配，导致内屏实际是 380×852，偏窄约 3.4%，看起来"细长"）；`.phone` 改为 `415×874px`（393/852 + 2×10px padding + 2×1px border），屏幕圆角改为 55px、外框圆角改为 66px（同心关系），灵动岛默认态改为 126×37px、圆角 18.5px（贴近官方 Dynamic Island 尺寸）。CSS 中加了一行来源注释，避免以后再变成手感数字。
- 修改前备份：`prototype/wireframes-high-fidelity.pre-processing-fix.bak.html`。
- 验证：通过控制台直接调用 `render('processing')` 分别走无冲突与冲突两条路径，全程 `window.onerror` 无报错；`planningPhase` 能推进到 4 或正确切到 `action`；对同一 `.progress-row` DOM 节点打自定义标记后触发一次阶段更新，标记仍在（证明是原地更新而非重建）；`getComputedStyle('.screen')` 确认宽高为 393×852。本次沙盒环境的浏览器预览对该目录下的文件只提供静态快照、无法截图核对像素级视觉效果，建议在真实浏览器中肉眼复核灵动岛与面板的衔接处。

## 60. 修复「面试已安排」状态图标与改期草稿取消操作（2026-08-01）

- 修改文件：`prototype/assets/figma-iphone/status-calendar.svg`、`status-bell.svg`、`status-note.svg`、`prototype/wireframes-high-fidelity.html`。
- **状态图标显示成空白方块**：三个 Figma 导出的图标文件里都多带了一层不透明背景矩形（`<rect width="119" height="119" fill="#F5F5F5"/>`），把图标本体整个盖住，「面试已安排」页三行（日历/出发提醒/准备事项）因此显示成空白方块而不是图标。对照没有这个问题的 `transport-walk.svg` 等文件，删除三个文件里多余的背景矩形。
- **改期草稿页的圆形返回按钮**：按反馈从图标按钮改为文字「取消」（`.btn.secondary`，与面板里其他次要操作风格一致），保留原有 `optionList` 跳转逻辑不变。

## 61. 通勤调整面板闪烁、交通图标裁切、菜单按钮裁切、返回建议改图标（2026-08-01）

- 修改文件：`prototype/wireframes-high-fidelity.html`，新增 `prototype/assets/figma-iphone/undo-alt.svg`。
- **「调整通勤安排」面板闪烁**：选择出发地点/交通方式的点击原来直接调用 `render('commute')`，而 `render()` 一开始就无条件 `ov.innerHTML=''`，导致每点一次都整体重建面板、重新播放进场动画。新增 `updateCommuteAdjust()`，在面板已存在时只原地更新标题文字、各选项的 `selected` 状态和计算结果，不再整体重建；点击后用自定义标记验证过是同一个 DOM 节点。
- **交通方式图标被裁切**：`.sf-transport` 容器只有 26×24px，但背景图是按 32×32px 绘制的，导致自行车等图标四周被裁掉一圈。容器改为 32×32px，与背景图尺寸一致；同时隐藏了图标上残留的旧版伪元素占位符（`:before`），避免叠加。
- **交通方式图标现在可点击**：「最终计划」页头部的交通方式图标从纯展示的 `<span>` 改成 `<button data-a="editCommuteAdjust">`，点击直接进入「调整通勤安排」面板（而不是先经过建议页），并加了 hover/按压反馈。
- **「面试已安排」右上角菜单按钮「···」被裁切**：按钮有默认的浏览器内边距，在 32px 圆形容器里把三个点挤出可视区域。改为 `padding:0`、缩小字号并收紧点间距，不再需要 `overflow:hidden` 硬裁。
- **「返回建议」改成图标**：原来是文字链接，现改成一个 undo 箭头图标按钮（`undo-alt.svg`，与其他 Figma 图标一致的实心线性风格，24×24 视窗）。当前环境没有可用的 Figma 文件链接，无法直接从你的 Figma 库导出同名的 `fi-rr-undo-alt`，这版是手绘的近似替代——如果和 Figma 里的原图不一致，之后把文件链接发我，我可以直接换成导出版本。
- 验证：全部 18 个可达状态 `render()` 均无报错；在「调整通勤安排」面板里切换交通方式，确认弹层 DOM 节点前后一致（未整体重建），标题与结果文字正确更新；`.sf-transport` 实测尺寸为 32×32px；`.task-menu-button` 内边距归零、文字未被裁切；undo 图标加载成功（natural size 24×24）。

## 62. 「面试已安排」内容精简 + 统一字号与间距规范（2026-08-01）

- 修改文件：`prototype/wireframes-high-fidelity.html`。
- **「面试已安排」内容精简**：按反馈去掉重复的执行结果展示（已加入日历/当天更新出发时间），面板只保留标题「面试已成功安排」和唯一一条待办「你还需要准备：携带作品集」（内容仍来自 `facts.preparation`，非写死），减少信息密度。日历等细节仍可通过「查看当天安排」「调整」入口访问，没有丢失，只是不在这一屏重复展示。
- **建立字号规范**：在 `:root` 新增三个字号变量 `--fs-title:20px`（标题）、`--fs-body:11px`（正文）、`--fs-caption:9px`（辅助文字/`--fs-eyebrow:10px` 用于 kicker 小标签），把 `.kicker`、`.popover h2/h3`、`.popover p/.sub` 以及分散在各面板里的次要说明文字（`.task-status small`、`.progress-pop small`、`.reminder-item p`、`.final-confirm .confirm-item small`、`.commute-answer p`、`.source-row small`、`.fact-list span`、`.rail-card small`、`.option-card span`）统一改用这几个变量，原来这批文字字号在 7–9px 之间随手取值，现在统一成一个尺寸。
- **修了一个一直存在但没被注意到的层级 bug**：`.kicker` 一直被更高优先级的 `.popover p{font-size:11px}` 覆盖（因为 kicker 都是 `<p class="kicker">`，标签选择器叠加类选择器的优先级比单独的类选择器高），导致"小标签"从来没有真正比正文小过。改成 `.popover p:not(.kicker)` 排除 kicker 后，kicker 现在正确显示为 10px，正文 11px，标题 20px，三级字号第一次实际生效。
- **对齐了几个跑偏的面板内边距/标题字号**：`.commute-dialog`/`.commute-pop`/`.reminder-pop` 的 padding 从 16–17px 统一为 18px（和其余弹层一致）；标题字号从 21px 统一为 20px（`--fs-title`）。
- 未处理：不同组件行内部的 padding（比如 `.task-status` 和 `.confirm-item` 的行高、左右留白）目前仍各有各的取值，这属于"组件内部密度"层面的设计判断，不是明显的手误型不一致，这次没有动，如果需要统一可以再单独过一版。
- 验证：全部 18 个可达状态无报错；实测 `.kicker`=10px、`.task-pop h2`=20px、`.task-status small`=9px、`.commute-dialog h2`=20px、`.commute-dialog` padding=18px，均与变量定义一致。

## 63. 颜色系统、来源可见性与「面试当天」间距（2026-08-01）

- 修改文件：`prototype/wireframes-high-fidelity.html`。
- **建立颜色变量**：新增 `--c-link:#8db7ff`（次要/文字类可点击操作的统一蓝色）与 `--c-defer:#9199a6`（"稍后处理"类操作的专用中性灰，不再用蓝色）。
- **统一了约 20 处散落的蓝色文字/链接颜色**：之前「调整」「来源与权限」「返回」「撤销安排」这类文字按钮在不同面板里分别用了 `#1769e8`、`#6f9cff`、`#6fa6ff`、`#76a2ff`、`#80aaff`、`#83aaff`、`#8db7ff`、`#94b4ff`、`#9fbfff` 九种不同的蓝色（其中还有一条 `!important` 规则和另外两条普通规则互相打架）。现在全部指向同一个 `--c-link` 变量。实测「面试已安排」里的「调整」按钮和「最终计划」里的「调整」链接，颜色现在完全一致（`rgb(141,183,255)`）。
- **「稍后决定」改用独立的中性灰**：不再是蓝色（之前是 `#9fbfff`，偏浅），现在用 `--c-defer`，和「调整」「来源与权限」这类蓝色可点击操作在视觉上明确区分开——蓝色=会带你去做点什么，灰色=先不处理。未改动的部分：冲突处理页里作为并列选项卡片出现的「稍后决定」（`.option-card`）保留原样，因为它和另外两个方案是同一层级的并列卡片，不是"链接/按钮"角色，强行套用同一套颜色反而会破坏那三个选项的视觉对等性；如果你希望这三张卡片也统一处理方式，可以再告诉我。
- **「面试已成功安排」上方新增来源标注**：原来的「···」只是通用溢出菜单（调整/来源与权限/管理通知/撤销），本身不表达"这项安排是从哪来的"。现在标题下方新增一行可点击的「✉️ 来自 Alex Chen 的邮件」，点击直接进入已有的「来源与权限」页（显示本次读取的 Mail/Calendar/Maps 范围）；「···」菜单本身保留，仍可访问调整/通知/撤销等其他操作。
- **「面试当天」间距与层级**：卡片间距 `.day-stack` 从 10px 增到 16px；卡片内边距从 15px 增到 18px；标签行（CALENDAR·今天 / AI·MAPS 实时交通）加粗并加了字间距，与标题拉开视觉差异；标题从 17px/margin:5px 调整为 18px/margin:7px 6px，与正文之间留出更明确的间距；正文行距从 1.45 提到 1.5；操作按钮组之前和上方文字零间距，现在加了 14px 的 `margin-top`。整体上标签/标题/正文/操作四层的字号、字重和间距现在能拉开明确梯度，不再挤在一起。
- 未处理（明确说明，避免误解为已经统一）：各组件"行"内部的左右留白/行高（`.task-status`、`.confirm-item`、`.reminder-item` 等）仍各有各的取值，这属于组件内部密度，上一版就标注过未处理，这次也没有动；主按钮（`.primary`）和次要填充按钮（`.secondary`）的颜色本身经核查其实已经是全局统一的（`.primary` 用 `--ios-blue:#0a84ff` 填充、`.secondary` 统一浅灰填充深色文字），这次没有发现需要改的地方，如果你看到具体某个按钮颜色不对，麻烦截图标出来，我再针对性查。
- 验证：全部 19 个可达状态（含 `taskMenu`）无报错；`later-action` 计算色值为 `rgb(145,153,166)`（= `--c-defer`），`adjust-link` 与 task 面板「调整」按钮计算色值均为 `rgb(141,183,255)`（= `--c-link`），两者明确不同；「来源」入口点击可正确进入 `sources` 状态；`.day-stack` gap 实测 16px、`.day-event h3` 实测 18px。

## 64. 按钮系统统一 + 「面试已安排」来源入口改版（2026-08-01）

- 修改文件：`prototype/wireframes-high-fidelity.html`。
- **发现并修了一个更根本的问题**：`.btn`（`.primary`/`.secondary`/`.text` 共用的基础类）从来没有设置过 `font-size`。按钮元素默认不继承页面正文字号，所以实测下来所有 `.btn` 一直渲染成浏览器默认的 **16px**，而像"最终计划"里的"调整"（`.adjust-link`，没用 `.btn` 类，单独写死 10px）完全是另一套。这就是你说的"同样是'调整'按钮，不同页面字号不一样"的直接原因——不是某个页面调错了，是从一开始就没有统一定义过。
- **新增按钮字号/字重规范**：`--fs-btn:15px`（主按钮、次要填充按钮统一用这个）、`--fs-link:11px`（"调整""稍后决定""来源与权限""撤销"这类文字类操作统一用这个，配合 `600` 字重）。「面试已安排」的"调整"、「最终计划」的"调整"、「需要决定」的"稍后决定"，三者字号现在都是 11px，高度从原来的 41px / 25px / 32px 收敛到 25–29px 区间（不同容器的 padding 略有差异，还没做到像素级完全相等，见下方"未处理"）。
- **颜色**：延续上一版的 `--c-link`/`--c-defer`，本轮重新确认"调整"类文字操作统一为 `--c-link`（浅蓝），"稍后决定"统一为 `--c-defer`（中性灰），且两者现在字号、字重、内边距逻辑也一致了，只有颜色不同——即"同一组件，颜色区分语义"，不再是"不同页面各写各的"。
- **「面试已成功安排」来源入口改版**：按反馈删除了标题下方"来自 XX 的邮件"这一整行（信息重复）。原来标题右侧的图标按钮"···"，改成和「最终计划」里"来源与权限"完全同一个组件（`.source-link` 同款样式：小号浅蓝文字、无边框），文案改为「来源与操作」以准确对应它实际打开的菜单内容（调整安排/来源与权限/管理通知/撤销安排）。位置仍在标题行右侧，但从"图标按钮"变成"文字链接"，和「最终计划」的入口现在是同一视觉语言。
- **补上缺失的 hover / pressed 反馈**：审查后发现 `.btn.primary`/`.btn.secondary` 之前完全没有 hover/active 状态（只有一条孤立的 `.secondary:hover` 规则，且已被后来的 `!important` 规则盖住失效）。现在统一加了：主/次按钮 hover 时轻微提亮、按下时轻微变暗+缩小（`scale(.985)`）；文字类按钮（调整/来源/撤销等）hover 时降低透明度、按下时更低，且都加了过渡动画，不再是瞬间跳变。
- 未处理（如实说明，避免误会已经完全统一）：
  - 按钮**高度**没有做到像素级一致（25–29px 之间还有 3–4px 差异），因为不同按钮所在的布局方式不同（有的在 CSS Grid 单元格里、有的是独立 `display:block` 元素），完全拉平需要统一布局结构本身，这次只统一了字号/字重/颜色/内边距逻辑，没有动更底层的布局方式，风险和收益不成比例，先没做。
  - 「最终计划」的操作区仍是"主按钮独占一行、调整链接另起一行"的堆叠布局，其余大多数面板是"主按钮+文字按钮并排"的左右布局——这是两种不同的排列方式，这次没有把它们合并成同一种，只统一了两种布局里按钮本身的字号颜色。如果你希望连排列方式也统一，需要再单独做一版，风险更高（涉及改动"最终计划"这个最关键页面的结构）。
  - 各组件"行"内部的左右留白/行高仍未统一，上两版都标注过，这次仍未处理。
- 验证：全部 20 个可达状态（含 `taskMenu`、`sources`）无报错；`.task-source-link` 文案「来源与操作」、`data-a="taskMenu"` 正确；旧的来源提示行已从 DOM 中移除；task/plan/action 三处"调整"/"稍后决定"字号实测统一为 11px；`.later-action` 颜色为灰、`.adjust-link` 与 task 面板"调整"颜色一致为蓝。

## 65. 「返回建议」换成真实 Figma undo 图标（2026-08-01）

- 修改文件：`prototype/assets/figma-iphone/undo-alt.svg`。
- 用你提供的 `fi-rr-undo-alt.svg` 原图替换了上一版手绘的近似图标，颜色改为白色（`fill="#fff"`）。CSS 未改动——按钮容器已有的 hover/pressed 样式和 19×19px 尺寸沿用不变。
- 「AI 规划中」进度面板的「收起」按钮按要求保持文字按钮，未改动。
- 验证：`assets/figma-iphone/undo-alt.svg` 加载成功（natural size 24×24，与 Figma 原图一致）；全部 18 个可达状态无报错。

## 66. 移除进度面板的「收起」按钮（2026-08-01）

- 修改文件：`prototype/wireframes-high-fidelity.html`。
- 按反馈删除「AI 规划中」进度面板底部的「收起」按钮（`.actions` 区块整块移除）。收起交互本来就同时绑定在灵动岛本体的点击上，删除按钮后功能不受影响——点击灵动岛仍然正确收起面板，后台规划继续。
- 顺手把面板底部说明文字从「收起后继续在后台规划」改成「点击灵动岛收起」，明确告诉用户现在唯一的收起方式是点灵动岛。
- 验证：全部 18 个可达状态无报错；`.progress-pop` 内确认不再有 `data-a="closeProgress"` 按钮；调用 `isl.onclick()` 后面板正确收起（`.island-sheet` 从 DOM 移除）。

## 67. 「要我帮你准备这场面试吗」改为可编辑范围选择（2026-08-01）

- 修改文件：`prototype/wireframes-high-fidelity.html`。
- 标题从「需要我帮你安排这场面试吗？」改为「要我帮你准备这场面试吗？」。
- 新增事实展示行：日期时间（`'+eventDateLabel()+' · '+eventTime()`）和地点，地点行右侧加了一个铅笔图标，点击进入已有的「修改提取信息」编辑页（复用 `editFacts`/`saveFacts`，没有新写一套编辑逻辑）。
- **原来的三条静态说明（看清时间地点/发现冲突/算好几点出门）改成三个可点击的 pill：「＋ 日历」「Map 路线」「设置提醒」，默认全部选中**。点击可以单独取消——这三个 pill 直接绑定的是 `calendarEnabled`/`prefs.travel`/`prefs.reminders`，和「最终计划」页里同一套变量共用，不是新建了一套独立状态。也就是说用户在最开始取消的项目，会一路带到最终计划确认页，那一项会显示为"未纳入"，不需要用户在后面再取消一次。已用脚本验证过这个联动：在 suggest 页取消「日历」后直接跳到 plan 页，日历那一行确认是 `off` 状态。
- 「＋」是留白的"添加更多"占位 pill，目前没有绑定任何行为（不是本次要做的功能，先按视觉稿放着，点了没反应）。
- 按钮文案从「忽略」改成「暂时不用」，逻辑不变（仍是 `data-a="mail"`，退回邮件）。
- 关于你截图 1 里还看到「收起」按钮：那是上一版（第 66 条）已经删掉的东西，代码里确认已经没有了，麻烦硬刷新一下本地页面（Ctrl+Shift+R）再看。
- 验证：全部 18 个可达状态无报错；三个 pill 默认 `selected`；点击「日历」pill 后 `calendarEnabled` 正确变为 `false` 且 pill 视觉同步取消选中；编辑图标 `data-a="editFacts"` 正确；操作按钮文案确认为「开始规划」「暂时不用」。

## 68. Pill 原地切换 + 日期/地点直接点击编辑（2026-08-01）

- 修改文件：`prototype/wireframes-high-fidelity.html`。
- **Pill 点击不再重建整个面板**：上一条（#67）新加的三个 pill，点击时是调用整块 `render('suggest')`，会把面板 `ov.innerHTML=''` 清空重建，重新播放一次进场动画——这正是之前处理过的"整个面板像是关闭再打开"的同一类问题，这次是我自己在加新功能时又引入了一次。现在改成点击只切换那一个 pill 按钮自身的 `selected` class，不再调用 `render()`，面板全程是同一个 DOM 节点（已用自定义标记验证过点击前后节点一致）。
- **日期和地点改成点击直接编辑，不再跳转到单独的编辑页**：原来地点行右侧的铅笔图标（点击后进入「修改提取信息」整页编辑）已经删除。现在日期时间行和地点行本身就是可点击区域，点一下原地变成输入框（`键盘直接弹出`），改完按 Enter 或点其他地方（失焦）就保存，不再需要"点图标→跳新页面→改→保存→返回"这一串步骤。
- 实现方式：新增 `data-edit-field` 标记两行，点击时把该行内容原地替换成 `<input>` 并聚焦；新增全局 `focusout`/`keydown(Enter)` 监听，统一调用 `commitInlineFact()` 写回 `facts` 对象并重新渲染，用 `dataset.committed` 标记防止两条监听同时触发时重复提交。
- 验证：全部 18 个可达状态无报错；点击「日历」pill 后确认弹层 DOM 节点前后一致（未整体重建）；模拟点击地点行→输入新值→触发 `focusout` 后 `facts.address` 正确更新；模拟点击日期行→输入新值→按 Enter 后 `facts.dateTime` 正确更新。有一点如实说明：这次验证 Enter 提交路径是在真实事件下测通的，失焦（点击别处）提交路径的逻辑和 Enter 走的是同一个 `commitInlineFact()` 函数，但受限于本次沙盒预览环境对程序化 `.blur()` 的事件支持不完整，没能在这个环境里用脚本触发真实 `focusout` 事件来端到端复测——这是标准浏览器 API（`focusout` 冒泡事件），在你自己的浏览器里点击输入框外部应该没问题，但建议你实际点一下确认。

## 69. 日期改用日历浮窗选择，时间保持输入（2026-08-01）

- 修改文件：`prototype/wireframes-high-fidelity.html`。
- 原来日期和时间是合在一起的一个字段，点击整行变成一个文本输入框。按反馈拆成两个独立控件：
  - **日期**：点击后在事实卡片下方展开一个小型日历浮窗（当月天数网格，当前选中日期高亮），点具体某一天直接选中、浮窗自动收起，不需要手动输入日期文字。星期几是用 `Date` 对象实时算出来的，不是写死的。
  - **时间**：保持点击变输入框的方式（键盘直接弹出，输入完按 Enter 或点别处保存），这部分沿用上一条（#68）已经验证过的 `commitInlineFact` 逻辑。
- 日历浮窗的开关、选日期后更新日期文字，都是**直接操作 DOM**（插入/移除浮窗元素、改文本节点），没有调用 `render()`，符合"面板不应该为每一步重新弹出"的要求。
- 验证：全部 18 个可达状态无报错；点击日期按钮后 `.mini-calendar` 正确出现；点选「15 日」后 `facts.dateTime` 正确更新为「7月15日（星期三）10:30」（星期几是真实算出来的，不是猜的），浮窗自动关闭；随后点时间、输入「16:45」、按 Enter，`facts.dateTime` 正确更新为「7月15日（星期三）16:45」，日期部分没有被覆盖。

## 70. 日历遮挡修复 + 面板收起动效 + 状态栏/灵动岛（2026-08-01）

- `.mini-calendar` 从 `position:absolute` 改回正常文档流（原来绝对定位会溢出弹层底部，盖住下面的 pills/按钮，这就是"日历被遮住"的成因）。
- `render(next)` 新增 `sameScreenRefresh` 判断：同一屏内刷新内容时跳过弹层的进场动画，不再每次 toggle 都像重新弹出一次——这是全局性修复，覆盖所有状态，不是只修了某一个屏。
- 新增 `collapseIslandSheet()`，灵动岛收起浮窗时播放退场动画（缩小+上移+淡出，200ms）后再移除节点，`closeProgress` 和点击灵动岛收起都走这个函数。
- 状态栏图标换成真实信号格/WiFi/电池 SVG（原来是几个 emoji 占位）；灵动岛下移到状态栏下方，不再重叠（`top:11px→34px`，配套的浮层衔接偏移量同步从 54px 改成 77px）。
- Figma 文件按反馈精简：删掉多余的 6 个空页面，只留一页，把 Color/Typography/Spacing/Radius token 做成可见的色板/字号样张/间距条/圆角样张（之前只建了 Variables，画布上看不到任何东西，所以你看着是空的）。
- AI API 接入：见对话回复，未写代码（这是产品决策，不是这次改的范围）。

## 71. 全流程按 AI 判断的 kind 联动（面试/会议/截止日期）（2026-08-02）

- `ai-proxy.js`：Gemini 集成从假设"都是面试"改成先分类 `kind`（appointment/meeting/deadline），schema 加 `eventTitle`/`location`，deadline 类没有地点/到达时间时老实写"无"/"不适用"，不瞎编。模型从 `gemini-2.0-flash`（这把 key 配额是 0）换成实测可用的 `gemini-flash-latest`。
- `wireframes-high-fidelity.html`：`suggest`/`plan`/`processing`/`task`/`today` 五个屏全部按 `facts.kind` 分支：
  - deadline：不出现地点行、Map 路线 pill、通勤相关文案；进度面板第三阶段叫"确认安排"；`today` 屏显示"今天到期"而不是导航建议；处理完直接跳到 `plan`，不经过 `commute`。
  - meeting：地点行显示会议链接；`plan` 屏用"会议链接"项替代"交通变化通知"；`today` 屏显示"加入会议"；同样跳过 `commute`。
  - appointment（默认/没接 AI 时）：行为不变。
  - `commute` 屏本身没改内容——它现在只在 appointment 场景才会被进入，deadline/meeting 走不到这里，不需要为不通的场景硬套通勤界面。
- 验证：appointment/deadline/meeting 三种 kind × 全部 18 个可达状态 = 54 组合，无报错；`processing` 在 `conflictResolved` 情况下实测走完全部计时器，deadline kind 确认最终落在 `state==='plan'`（不是 `commute`）。

## 72. AI 输入口从原生 prompt 改成侧栏表单 + 修复无提示问题（2026-08-02）

- 左侧栏新增真正的 textarea + 3 个示例按钮（面试/线上会议/账单截止）+「AI 分析」按钮，取代原来丑陋的浏览器原生 `prompt()` 弹窗。
- 修了"点了没反应"的真问题：代理没启动时，`fetch` 实测要 ~2.4 秒才会失败（浏览器等本地端口连接超时），之前用 `alert()` 报错、点击瞬间又没有 loading 态，很容易让人以为"点了没用"。现在按钮点击立刻变成"AI 分析中…"，结果不管成功失败都在按钮下方常驻显示（成功绿字，失败红字带具体原因），不再是一闪而过的弹窗。
- 验证：真实点击（不是直接调函数）测过完整等待时间，确认失败态会显示「✗ 连不上 http://localhost:8787（Failed to fetch）——代理没启动，或者没跑在这个端口」，按钮正确恢复可点击。
