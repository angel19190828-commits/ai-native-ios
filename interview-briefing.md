# AI 原生 iOS 原型 — 面试材料（喂给 ChatGPT 用）

给 ChatGPT 的提示语建议：把这份文件贴给它，问它「帮我把这个项目讲成一个 3 分钟的面试故事，突出设计判断和迭代过程」，或者「针对这份材料，面试官可能会问什么，我该怎么答」。

---

## 项目是什么

一个探索"AI 原生 iOS 系统"应该长什么样的高保真交互原型（非生产代码，纯设计原型，HTML/CSS/JS 手写）。核心问题：当 AI 可以代表用户跨 App 完成任务时，系统应该在什么时候打断用户、什么时候在后台自动做、什么时候必须停下来等用户决定。

两个具体场景验证同一套设计语言：
1. **AI 帮忙安排面试** — 从识别邮件邀请，到检查日程冲突、规划通勤、设置提醒，最后确认执行。
2. **AI 整理 WhatsApp 群聊安排聚餐** — 从读群聊，到跨 App 查日历、订餐厅、发群消息确认。

## 核心设计原则（贯穿全程，适合面试时反复强调）

- **AI 在场但不占场**：日常状态用灵动岛承载，只有关键决定才升级为全屏/半屏面板。
- **识别 ≠ 执行**：AI 可以主动发现、主动建议，但真正写入系统（发邮件、订位、发消息）之前必须有一次明确确认。
- **自动化普通步骤，打断关键节点**：日常、可恢复的步骤（查日历、算路线）AI 自动做完；冲突、发送、写入这类不可逆或涉及他人的动作，必须停下来问。
- **无回应 ≠ 同意**：用户长时间不理会时，系统暂停、保留状态、稍后提醒一次，绝不擅自做主。
- **一个任务只用一套界面语言**：同一个任务从"发现"到"完成"全程复用同一个容器/组件，只通过状态和内容表达阶段变化，不跳到无关新页面。

## 迭代过程概览（按阶段，用于讲"设计怎么演化的"）

### 第一阶段：验证系统行为（低保真，wireframes.html）
- 从静态线框图做成可交互 iPhone 模拟，8 个关键状态可直接跳转。
- 把灵动岛定为 AI 的轻量入口；加入展开 motion 表达"有事发生"。
- 确立"后台规划=浮窗、不占满屏"、"日程冲突=必须打断"两条基本规则。

### 第二阶段：Approval-first 对照版本（wireframes-approval.html）
- 做了一个"每步都要用户批准"的独立版本，与"AI 自动推进"版本对照，验证两种自主程度的取舍。
- 经过约 40 轮细节迭代：冲突处理具体化（联系对方改期 / 保留 / 稍后决定三选一）、通勤从表单改成三步对话式流程、偏好设置从固定问答改成 AI 推荐+可关闭、主状态从 12 个收敛到 6 个。
- 期间修了大量视觉系统性问题：按钮从来没统一过字号（发现全局都是浏览器默认 16px）、9 种不同的蓝色链接颜色统一成一个变量、图标资源对齐 Figma 导出。
- 关键判断：**结果范围先于技术实现**——授权前只说"我会做到什么"，不说"我要读哪个 App、写哪个字段"；细节在授权后才逐步展示。

### 第三阶段：高保真视觉 + AI 接入（wireframes-high-fidelity.html）
- 接入真实 Figma 视觉资源（iPhone 设备框架、图标）。
- 接入 Gemini API 做真实的邮件内容分类（约会 / 会议 / 截止日期三种 kind），不再是写死的面试场景，UI 按 kind 分支显示不同信息（比如 deadline 不显示地点和通勤）。

### 第四阶段：V2/V3 对话式规划入口（wireframes-high-fidelity-v2.html）
- 把"配置面板"改成"对话式 Sheet"：AI 说一句话总结建议，用户可以直接对话式修改，而不是填表单。
- 经过约 15 轮迭代收敛出目前的核心组件：浮动的玻璃质感对话框（composer）+ 可展开的信息卡片面板（task-panel）+ 灵动岛。
- 建立统一 typography scale，解决了"同样的按钮在不同页面字号不一样"这类历史遗留的系统性不一致问题。

### 第五阶段（最近一周，本次会话）：设计系统统一 + 多选 + 导航审查 + WhatsApp 新流程
详见下一节——这是目前最新鲜、最适合展开讲的部分。

## 本周最新更新（面试最该讲的部分）

### 1. 玻璃质感设计语言统一到全部页面
- 之前只有"最终计划"这一个页面用了毛玻璃（backdrop-filter blur）质感，其余页面还是纯色。这次把这套视觉语言铺到了新旧两套渲染系统（`task-conversation.js` 新系统 + `conversationShell()` 旧系统）覆盖的全部约 18 个页面。
- 顺带修了好几个"看起来是同一个问题、实际是分别踩坑"的 bug：
  - 对话输入框圆角一直没生效（CSS 优先级问题，声明了但被默认规则覆盖）。
  - 一个原生 `<button>` 元素因为只写了 `border-bottom`、没写完整 `border`，被浏览器默认灰色按钮边框污染视觉。

### 2. 悬浮输入栏的正确物理行为（参考 iOS 系统搜索框）
- 平时固定停在屏幕底部安全区上方，跟卡片内容多高完全无关；打字时贴着键盘上沿升上去。
- 中间踩了两次坑才落地：先做成"跟内容一起排版"导致内容短时输入栏悬在半空、内容长时被键盘挡住；又做成"面板整体拉伸到固定高度"导致内容短时下面留一大块空的深色死区。最终方案是"面板内容自己多高就多高 + 输入栏完全独立悬浮 + 面板底部做柔和渐隐（不是硬切边）"，三者互不依赖，长短内容都不会露馅。

### 3. 计划卡片支持多选
- 原来"面试时间 / 面试地点 / 交通方式"这些卡片只能单选一项来编辑，改成了可以多选（比如同时选中"面试时间"和"交通方式"，然后打一句话同时改两处）。
- 有个容易忽略的交互细节：多选之后，"再点一次已经选中的项"不能被理解成"取消选中"（这是很多多选组件的默认行为），因为原有的"重新点选同一项来确认当前编辑目标"的用法要保留。最后方案是"再点一次 = 把它标记为最新目标"，真正的取消只能通过每个选项自己的 × 按钮完成。

### 4. 决策弹窗自动弹出 + 默认选项预填
- 之前需要用户决定的弹窗（比如日程冲突）建好之后是收起状态，用户容易没注意到，被灵动岛挡住。改成有决定要做时自动弹出。
- 有明显默认选项时（比如"保留面试"），提前把它填进输入框，用户直接按发送就等于确认，不用先点一下建议标签再发送。

### 5. 冲突时间轴精确标注
- 原来"10:00–11:30"是合并成一段文字。改成时间轴上用虚线连接到实际的时间条边缘，起止时间精确标注在对应位置（参考了系统日历冲突提示的视觉）。

### 6. WhatsApp 聚餐流程：AI 生成草稿、人工确认发送（而不是自动发消息）
这是本次最大的一个产品判断变化：
- 原来的设计是"用户确认聚餐安排后，AI 自动完成订位、写日历、发群消息"三件事全自动。
- 改成：AI 自动完成订位和写日历（这两项不涉及他人、可撤销），但到"通知群里"这一步，AI 只生成一条文字草稿、**放进 WhatsApp 的输入框里，不自动发送**，必须用户自己按发送键。
- 设计判断：**代表用户对外发消息，属于"影响他人、AI 不该单方面替用户决定"的动作**，即使内容是 AI 生成的，最后一下"确认要不要说这句话"必须留给人。这和最初"识别 ≠ 执行"的原则是一脉相承的，只是这次具体应用到了"对外沟通"这个更敏感的场景。

### 7. 全局返回/退出导航审查（进行中，尚未实施）
- 系统性梳理后发现：全 App 一共有 **8 种不同的"返回/退出"视觉表现**（图标返回、图标+文字返回、纯文字链接、"完成"按钮、收起图标、下滑手势、点击外部关闭、点灵动岛），但实际上功能冲突的地方很少——真正的问题是"同一个动作在不同页面长得不一样"，不是逻辑冲突。
- 顺带挖出两个真实遗留 bug：一份 `taskSheetHeader()` 函数被同名定义覆盖、从未生效；一个 `closeAdjust` 按钮的目标变量在当前代码里已经没有任何按钮会写入它（死代码）。
- 下一步计划：收敛成两种模式——左上角"‹"表示"回上一级"，底部主按钮表示"完成/确认"，其余全部去掉。

## 可能会被问到、建议提前想好怎么答的问题

- "为什么灵动岛而不是通知栏/横幅？" → 灵动岛常驻可见但极小，横幅式提醒会打断当前操作或被划走后就找不回来；灵动岛保证"AI 的状态"始终有一个稳定、可点回去的落点。
- "怎么决定哪些操作要自动、哪些要问？" → 判断标准不是"复杂度"，而是"可逆性"和"是否影响他人"：只读查询、可撤销的写入（日历事件）自动做；不可逆或对外的动作（发消息、最终执行）必须确认。
- "这套原则会不会不通用，换个场景就崩？" → 用两个差异很大的场景（个人日程 vs 多人群聊协调）验证过同一套原则依然成立，尤其是 WhatsApp 场景把"对外沟通"这个更敏感的维度也纳入了同一套框架而没有另起一套规则。
- "做了这么多轮小修小改，怎么保证不是无意义地打磨？" → 相当一部分"小修"其实是在修**系统性**问题（比如全局字号从没统一过、9 种蓝色统一成 1 个变量），而不是孤立的像素级调整；这类修复往往一次改动能同时修好十几个页面的同一个毛病。
