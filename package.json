{
  "name": "ai-native-ios",
  "version": "1.0.0",
  "private": true,
  "description": "AI-native iOS task planning concept — GitHub Pages frontend + Vercel API backend (Gemini proxy).",
  "scripts": {
    "dev:api": "vercel dev",
    "dev:local-proxy": "node prototype/ai-proxy.js"
  }
}
